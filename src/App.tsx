import { Typography } from '@mui/material';

export const App: React.FC = () => {
    return (
        <>
            <Typography variant="h3" sx={{ fontWeight: 900 }}>
                Pokedex
            </Typography>
        </>
    );
};
